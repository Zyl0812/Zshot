<?xml version="1.0" encoding="utf-8" ?>
<sf:WindowEx x:Class="Starshot.Features.Screenshot.RegionCaptureWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mgcux="using:Microsoft.Graphics.Canvas.UI.Xaml"
             xmlns:sf="using:Starshot.Frameworks"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">

    <Grid x:Name="RootGrid"
          Background="Transparent"
          KeyDown="RootGrid_KeyDown">
        <mgcux:CanvasSwapChainPanel x:Name="Canvas"
                             PointerPressed="Canvas_PointerPressed"
                             PointerMoved="Canvas_PointerMoved"
                             PointerReleased="Canvas_PointerReleased"
                             PointerCaptureLost="Canvas_PointerCaptureLost" />
        <Canvas x:Name="ChromeLayer">
            <Border x:Name="ToolbarBorder"
                    Padding="6"
                    Background="#E0222222"
                    CornerRadius="6"
                    Visibility="Collapsed">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <Button Click="Tool_Click" Content="选择" Tag="select" />
                        <Button Click="Tool_Click" Content="矩形" Tag="rect" />
                        <Button Click="Tool_Click" Content="椭圆" Tag="ellipse" />
                        <Button Click="Tool_Click" Content="直线" Tag="line" />
                        <Button Click="Tool_Click" Content="箭头" Tag="arrow" />
                        <Button Click="Tool_Click" Content="画笔" Tag="pen" />
                        <Button Click="Tool_Click" Content="文字" Tag="text" />
                        <Button Click="Tool_Click" Content="马赛克" Tag="mosaic" />
                        <Button Click="Tool_Click" Content="序号" Tag="number" />
                        <Button Click="Undo_Click" Content="撤销" />
                        <Button Click="Redo_Click" Content="重做" />
                        <Button Click="Clear_Click" Content="清空" />
                    </StackPanel>
                    <StackPanel Margin="0,4,0,0" Orientation="Horizontal" Spacing="4">
                        <Button Click="Ocr_Click" Content="OCR" />
                        <Button Click="Translate_Click" Content="翻译" />
                        <Button Click="LongCapture_Click" Content="长截图" />
                        <Button Click="Copy_Click" Content="复制" />
                        <Button Click="Save_Click" Content="保存" />
                        <Button Click="Complete_Click" Content="完成" />
                        <Button Click="Cancel_Click" Content="取消" />
                        <TextBlock x:Name="HintText"
                                   VerticalAlignment="Center"
                                   Foreground="White"
                                   Visibility="Collapsed" />
                    </StackPanel>
                </StackPanel>
            </Border>
            <Border x:Name="LongCaptureBar"
                    Padding="8"
                    Background="#E0222222"
                    CornerRadius="6"
                    Visibility="Collapsed">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock x:Name="LongCaptureStatus"
                               VerticalAlignment="Center"
                               Foreground="White"
                               Text="请滚动页面以继续截图" />
                    <Button Click="LongCaptureFinish_Click" Content="完成" />
                    <Button Click="LongCaptureCancel_Click" Content="取消" />
                </StackPanel>
            </Border>
            <Border x:Name="TextInputBorder"
                    Padding="4"
                    Background="#F0222222"
                    CornerRadius="4"
                    Visibility="Collapsed">
                <TextBox x:Name="TextInputBox"
                         MinWidth="180"
                         KeyDown="TextInputBox_KeyDown" />
            </Border>
            <Border x:Name="ResultPanel"
                    Width="280"
                    MaxHeight="240"
                    Padding="8"
                    Background="#F0222222"
                    CornerRadius="6"
                    Visibility="Collapsed">
                <StackPanel Spacing="6">
                    <TextBlock x:Name="ResultTitle" Foreground="White" Text="OCR" />
                    <TextBox x:Name="ResultText"
                             MinHeight="80"
                             AcceptsReturn="True"
                             IsReadOnly="True"
                             TextWrapping="Wrap" />
                    <Button Click="ResultCopy_Click" Content="复制" />
                </StackPanel>
            </Border>
        </Canvas>
    </Grid>

</sf:WindowEx>
